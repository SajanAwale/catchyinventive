on:
  push:
    branches:
      - main

name: 🚀 Deploy Laravel app on push

jobs:
  web-deploy:
    name: 🎉 Deploy to Production Server
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Checkout latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files to production server
        uses: SamKirkland/FTP-Deploy-Action@4.1.0
        with:
          server:  ftp.pavilionsolution.com.np
          username:  bidhya@api.pavilionsolution.com.np
          password:  Bidhya@@1@@
          local-dir:  ./
          server-dir: /home/pavilion/api.pavilionsolution.com.np